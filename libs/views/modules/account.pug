extends ../layout


block content
	- var title = "Your account"
	- var sub = user.name
	.contain
		include ../static/nav.pug

		if user
			img(src=user.avatarUrl, height="80")
			p=user.id
			p=user.login
			p=user.name
			p=user.type
			p=user.url
			a.logout(href="/auth/logout") Logout
