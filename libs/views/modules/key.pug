extends ../layout

block content
	- var title = "Dashboard"
	- var sub = "Overview"
	.contain
		include ../static/nav.pug
		section#key_info
			.info Name: #[span=key.name]
			.info ID: #[span=key.id]
			.info.float-right Domain: #[span=key.domain]
		section#trackers
			.grid-row
				.xs-12.m-6
					ul#clicks
						li.header #[h5 Click trackers]
							div.search #[i.ion-ios-search-strong] #[.filter.hidden(contenteditable="true")]
							if hasClickTrackers
								each val, index in clicktrackers
									li(data-click=index) #{index}
										span #{val}
							else
								li.no_trackers No click trackers

				.xs-12.m-6
					ul#hits
						li.header #[h5 Hit trackers]
							div.search #[i.ion-ios-search-strong] #[.filter.hidden(contenteditable="true")]
							if hasHitTrackers
								each val, index in hittrackers
									li(data-hit=index) #{index}
										span #{val}
							else
								li.no_trackers No hit trackers


block footer
	script(src="/socket.io/socket.io.js")
	script.
		const trackR = `/track_#{trackKey}`
